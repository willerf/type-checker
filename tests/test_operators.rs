
fn or(x, y) {
    return x || y;
}

fn and(x, y) {
    return x && y;
}

fn eq(x, y) {
    return x == y;
}

fn ne(x, y) {
    return x != y;
}

fn lt(x, y) {
    return x < y;
}

fn gt(x, y) {
    return x > y;
}

fn le(x, y) {
    return x <= y;
}

fn ge(x, y) {
    return x >= y;
}

fn plus(x, y) {
    return x + y;
}

fn minus(x, y) {
    return x - y;
}

fn times(x, y) {
    return x * y;
}

fn divide(x, y) {
    return x / y;
}

fn mod(x, y) {
    return x % y;
}

fn not(x) {
    return !x;
}

fn main() {}
